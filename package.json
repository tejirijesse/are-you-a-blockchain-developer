{
  "name": "are-you-a-blockchain-developer",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat project for registering and tokenising the ALU logo on Ethereum.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js",
    "hash:logo": "node scripts/hash-logo.js",
    "frontend:serve": "node scripts/serve-frontend.js",
    "screenshots:build": "python3 scripts/make_screenshots.py",
    "report:build": "python3 scripts/build_pdf.py"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
